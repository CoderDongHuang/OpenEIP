server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: platform-auth

logging:
  level:
    com.openeip: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
