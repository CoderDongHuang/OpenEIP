# v0.2.0-alpha Release Checklist

> 状态：Local verification passed; remote review pending | 目标版本：`v0.2.0-alpha` | 验证日期：2026-07-22

## Release Scope

本版本发布单节点 MVP：Auth/RBAC、File Upload、受限 OCR/Document Parsing、Knowledge Base、
确定性 Embedding/RAG、Streaming Chat 和 Constrained Agent。生产模型、Kafka/Milvus 默认部署、
远程 MCP、多机高可用、备份恢复和生产多租户隔离不在本次承诺内。

## Required Verification

| 检查 | 状态 | 证据 |
|---|---|---|
| Java clean/check/build、覆盖率、Checkstyle、SpotBugs、Spotless | Pass | 102 tasks + 两个 Java Spike |
| Python Ruff、Mypy、Unit/Integration、覆盖率、6 个 Benchmark | Pass | 161 tests + 6 benchmarks，97.00% |
| Frontend/Website audit、lint、format、test、typecheck、build | Pass | Frontend 12 tests；无 HIGH/CRITICAL |
| Foundation/Spike Compose 与证据校验 | Pass | 6 份 Compose + `spike/validate_results.py` |
| Docker Compose 三镜像构建和五服务健康 | Pass | `docker compose up -d --build --wait` |
| v0.2 关键业务整栈冒烟 | Pass | `python scripts/release_smoke.py` |
| Trivy 仓库、Java Runtime、Python Runtime 无 HIGH/CRITICAL | Pending | GitHub `Security Scan` |
| 六项远程 CI | Pending | 发布 PR checks |
| 独立 Code Review Approve | Pending | 发布 PR review |

## Full-stack Acceptance

发布冒烟必须通过以下真实容器边界：

1. Gateway、Frontend、Java、Python 和 MySQL 健康。
2. 用户注册、登录、`/me` 与非管理员 RBAC 拒绝。
3. 文件上传、下载摘要一致、知识库创建与文档关联。
4. OCR、解析、Embedding、RAG 内部 API 共用同一身份和请求上下文。
5. Java Chat SSE 经 Python 返回 grounded answer 和 Citation，并持久化消息历史。
6. Java Agent SSE 执行显式授权的 `knowledge.search`，产生工具生命周期和完成事件。
7. 删除关联、知识库和文件后资源不可再访问。

## Governance Note

模块 PR #52-#59 已通过六项 CI 并合并，但 GitHub 没有记录独立 `Approve`。发布 PR 必须获得
非作者 Reviewer 的正式批准，或者先通过公开治理变更明确 Bootstrap Maintainer 的临时审查规则；
不得把维护者自查伪装成 GitHub Approval。

## Publish Procedure

1. 完成本清单并将状态改为 `Verified for release`。
2. 合并 `release/v0.2.0-alpha` 到 `main`。
3. 从合并提交创建并推送 `v0.2.0-alpha` 标签。
4. Release Workflow 重新运行完整 CI 和整栈冒烟。
5. Workflow 发布 Java、Python、Frontend GHCR 镜像并创建 GitHub Pre-release。
6. 验证镜像、SBOM、Provenance、Release Notes 和下载入口。
