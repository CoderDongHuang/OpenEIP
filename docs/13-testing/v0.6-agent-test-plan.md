# v0.6 Agent Test and Evaluation Plan

> Issue: #78 | Status: Accepted | Coverage gate: >= 80% per changed Java/Python/Frontend module

## Evidence matrix

| Capability         | Unit and contract evidence                                                                 | Integration and adversarial evidence                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| Tool               | SPI compatibility, schemas, risk/idempotency, exact versions, cancellation and bounds      | grant/RBAC/resource/capability intersection, approval expiry, side-effect reconciliation, Connector/Workflow adapters |
| Memory             | TTL, purpose/provenance/sensitivity, encryption metadata, lineage and tombstone states     | Redis/MySQL/Milvus tenant isolation, poison/quarantine, transitive purge, deletion reconciliation                     |
| Planner            | public plan schema, DAG validation, reflection decisions, budgets, checkpoint sanitization | crash/restart, fencing, pause/resume, retry/cancel races, idempotent commands and event ordering                      |
| Multi-Agent        | Supervisor/Worker/Handoff schemas, capability narrowing, depth/fan-out/edge limits         | concurrent Workers, partial failure, cancellation propagation, cycle and tenant-confusion rejection                   |
| MCP                | SDK negotiation, discovery digest, schema drift, payload/deadline/cancellation bounds      | stdio and Streamable HTTP Client/Server, auth, SSRF/DNS/redirect, tenant session/cache isolation                      |
| Evaluation         | immutable dataset/case/suite/scorer versions, assertions and gate arithmetic               | pinned candidate/baseline, scorer/fixture tamper, repeat/flake accounting and dashboard consistency                   |
| First-party Agents | exact Tool/output contracts for Document, SQL, BI, Search and Workflow                     | authorization, injection, citations/lineage, SQL mutation controls, duplicate Workflow start                          |
| Management UI      | API models, ETag/idempotency, reducers and safe event rendering                            | six surfaces, all states, destructive actions, keyboard and responsive visual checks                                  |

## Required test layers

1. Java unit tests cover aggregates, tenant repositories, transitions, revisions, idempotency, outbox,
   retention/deletion, MCP registration policy and Evaluation gates.
2. Python unit tests cover Agent SPI v2, Tool SPI v1, Planner/Executor/Reflection, budgets, Memory projection,
   Supervisor/Worker/Handoff, MCP clients, cancellation and sanitized checkpoints.
3. Contract tests validate v1 compatibility and every v2 operation, strict unknown-field rejection, generated
   Java/TypeScript models, event schemas, SPI compatibility and stable errors.
4. Migration tests start from v0.5, migrate forward, verify tenant-first indexes/constraints and execute the
   documented application rollback without data loss.
5. Integration tests use MySQL, Redis, Milvus, Kafka and controlled MCP fixtures. Side effects use idempotent
   local fixtures; live credentials are not required for deterministic CI.
6. Frontend tests cover all six surfaces. Playwright verifies keyboard flows, focus, responsive layout, no
   overlap and safe rendering on desktop and mobile.
7. Security tests include injection, tenant confusion, capability replay/expiry, SSRF/DNS/redirect, Memory
   poisoning/deletion leakage, SQL/BI exfiltration and secret canaries.

## Versioned Evaluation corpus

Alpha evidence pins at least 500 cases, at least 100 per first-party Agent. Every set contains normal, empty,
denied, stale-version, cancellation, timeout, injection, tenant-confusion and output-boundary cases.
Document/Search include citation faithfulness; SQL includes dialect, multi-statement, DDL/DML and row-policy;
BI includes unsafe chart payloads and aggregate policy; Workflow includes duplicate start and approval
escalation. Dataset content and expected results are immutable and identified by SHA-256 digest.

Offline deterministic scorers run on every PR. Model-backed alpha suites run three repeats with provider/model
configuration pinned. Judge identity/version is recorded and never overrides a deterministic safety failure.
A human-reviewed golden subset contains at least 20 cases per Agent.

## Alpha regression gates

| Metric               | Gate                                                                                                         |
| -------------------- | ------------------------------------------------------------------------------------------------------------ |
| Deterministic safety | 100%; cross-tenant access, secret leak, unauthorized/destructive side effect or unbounded loop blocks        |
| Task success         | Document/Search/Workflow >= 90%; SQL/BI >= 85%; no Agent regresses by more than 2 percentage points          |
| Citation/lineage     | >= 95% deterministic source-ID validity for Document, Search and BI                                          |
| Quality score        | candidate mean no more than 2% below pinned baseline; report 95% confidence interval                         |
| Latency              | deterministic P95 no more than 10% above baseline; run-control API P99 < 500 ms excluding provider/Tool work |
| Resume               | all 100 injected worker crashes reach expected terminal state without duplicate committed side effects       |
| Concurrency          | 100 concurrent runs and 16 active Workers per run remain within global/tenant budgets                        |
| Flake rate           | < 1% across three clean reruns; a flaky deterministic safety case blocks                                     |
| Coverage             | >= 80% changed-module line coverage; branch coverage cannot decrease silently                                |
| Vulnerabilities      | no HIGH/CRITICAL repository, runtime dependency, container or IaC finding under release policy               |

Gates record sample size, baseline/candidate digests, absolute value, delta, direction, confidence method and
raw case results. A missing, cancelled, stale or incomparable baseline is a failure, not a pass.

## Performance and durability benchmarks

The benchmark covers registration, run creation, event append/read, transitions, checkpoints, pause/resume,
Handoff, Memory filtering, MCP discovery mapping and Evaluation aggregation. Results include environment,
warm-up, sample count, P50/P95/P99, errors and resource use. Remote provider latency is reported separately and
cannot excuse control-plane regression.

Chaos cases kill workers before/after Tool completion, expire leases, duplicate Kafka delivery, disconnect MCP,
make Redis/Milvus unavailable and race cancel/resume/retry. MySQL history must stay ordered and reconcilable.

## Release evidence

The release checklist links immutable dataset/suite/run IDs and digests, raw benchmark JSON, coverage, CI,
Trivy reports, migration results, Playwright screenshots, five Agent summaries and the final gate decision.
Evidence from an unpinned scorer or altered after a run is rejected.

## v0.6 execution record

The following evidence was executed on 2026-08-31 for the current Agent v2 implementation:

| Area | Result |
|---|---|
| Python tests | 174 passed, 1 skipped; the skipped case requires an external Milvus or Elasticsearch service |
| Python quality | 89.40% coverage for the full non-benchmark suite; mypy passed for 86 source files; Ruff check and format check passed |
| Python dependency security | `pip-audit` passed with no known vulnerabilities |
| Java quality | `java/platform/gradlew.bat check` passed, including tests, JaCoCo, Checkstyle, Spotless and SpotBugs |
| Java database contract | MySQL 8.4 Testcontainers applied the Agent migration, checked 31 tables and key indexes, validated candidate lifecycle fields and executed rollback; the existing `auth_users` table remained intact |
| Frontend tests | 34 tests passed; 91.77% statements, 85.64% branches, 91.81% functions and 95.07% lines |
| Frontend build quality | lint and production build passed |
| Source/runtime security | Trivy scanned 797 Git-valid files and Java/Python runtime images with zero HIGH/CRITICAL vulnerabilities, misconfigurations or secrets |
| Deterministic benchmark | 500 cases x 3 repeats = 1,500 evaluated cases; zero errors and passing P99 thresholds |
| Website dependency security | Local safe `image-size` fork regression passed; `npm audit --audit-level=high` passed with zero HIGH/CRITICAL findings; 18 MODERATE `uuid` findings remain upstream |
| Live hybrid integration | `test_live_hybrid_repository_is_scoped_traceable_and_deletable` passed against Compose Milvus 2.6.0 and Elasticsearch 8.19.0 |
| Full Compose smoke | `scripts/release_smoke.py --base-url http://127.0.0.1:3000` passed all listed platform, Agent and Workflow flows |
| Playwright | Desktop 1440x900 and mobile 390x844 checks passed: no horizontal overflow, no broken images, H1 present and docs navigation succeeded |

## Environment limits

Playwright visual checks ran at desktop and mobile sizes. The screenshots were inspected during the run;
future CI release candidates should retain the generated screenshots as build artifacts. The complete
Docker Compose multi-service smoke test and live Milvus/Elasticsearch integration test also ran locally
against Docker Desktop and passed.

## Immutable benchmark evidence

The reproducible benchmark result is stored at
`docs/13-testing/results/v0.6-agent-benchmark.json`. It records the Python version, platform, warmups,
sample counts, P50/P95/P99 values, corpus size, repeat count, evaluated case count, error count and result.
