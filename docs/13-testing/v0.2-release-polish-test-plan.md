# v0.2 Product and Release Polish Test Plan

> Issue: #63 | Gate: coverage >= 80%, no benchmark regression, no HIGH/CRITICAL finding

## Automated Coverage

- Knowledge domain: FAILED/READY reset, invalid reset states, bounded retry history, and failure-code clearing.
- Knowledge application: VIEWER denial, EDITOR retry, content digest validation, and processing completion.
- API contract: additive `/processing/retry` path and unchanged response envelope.
- Frontend: authenticated API encoding, recovery endpoint, SSE cancellation/error behavior, lint, typecheck,
  production build, and core coverage.

## Full-stack Acceptance

1. Register and sign in through the public gateway; inspect the current identity as a standard user.
2. Upload a TXT source and a PDF; verify the PDF is labeled stored-only before knowledge processing.
3. Create a knowledge base, attach and process text to READY, then receive grounded Chat citations.
4. Restart the Python service, rebuild the READY document, and verify Chat works again.
5. Cancel a Chat stream and restore the last question into the composer without duplicate submission.
6. Execute the constrained Agent with an explicit knowledge base, tool allowlist, and visible timeline.
7. Verify a VIEWER cannot invoke processing/retry through the public API.
8. Verify desktop and 390 x 844 mobile layouts have no incoherent overlap or horizontal page overflow.

## Performance and Security

- Keep each feature route below 75 KiB gzip and no emitted JavaScript asset above the existing 150 KiB
  application budget, excluding framework chunks already accepted by the baseline.
- Run Java/Python module benchmarks and compare machine-readable evidence with the v0.2 baseline.
- Run dependency audits and Trivy source/runtime scans with zero HIGH/CRITICAL findings.
- Inspect built frontend assets for internal service tokens, raw document content, and external scripts.

## Verification Record

Verified on 2026-07-24:

- Java all-module `check build` passed after a successful 28-test Knowledge rerun against MySQL
  8.4.10 and embedded Kafka. Checkstyle, SpotBugs, Spotless, and coverage gates passed.
- Frontend ESLint, Prettier, production build, and 27 tests passed at 90.80% statement coverage.
- The five-service Compose stack and `scripts/release_smoke.py` passed on the hardened MySQL 8.4.10
  image using the existing data volume. READY rebuild and failed-source retry were also exercised.
- Trivy scanned a 564-file Git snapshot plus all five runtime images with zero HIGH/CRITICAL findings.
- Desktop 1440 x 900 and mobile 390 x 844 browser acceptance found no page overflow, out-of-bounds
  controls, or console errors across Auth, Documents, Knowledge, and Access.
