# v0.3 Knowledge Test Plan

> Issue: #65 | Coverage gate: >= 80%

| Area | Required evidence |
|---|---|
| Parsing | Real PDF/DOCX/PPTX/XLSX fixtures, encrypted/corrupt/archive-limit rejection, page mapping |
| Embedding | Deterministic and mocked OpenAI-compatible batches, normalization, secret redaction |
| Full text | Exact-term ranking and tenant/base isolation against Elasticsearch |
| Vector | Nearest-neighbor and filter-before-rank behavior against Milvus |
| Hybrid | Stable RRF order, tie-breaking, empty-side behavior |
| Citation | Excerpt/page/range propagation and forged citation rejection |
| Java | membership authorization, internal gateway validation, MIME upload/processing |
| Frontend | mode request, result rendering, citation parser and responsive layout |

Benchmarks record P50/P95/P99 for the existing parsing and embedding baselines plus 1,000-record scoped
hybrid retrieval. Local deterministic retrieval targets P99 < 50 ms. A separate live integration test
must exercise Milvus 2.6 and Elasticsearch 8.19 for scoped upsert, both retrieval paths, citation metadata,
and delete; external provider latency is reported separately and is not hidden by the local benchmark.
